{
  "name": "@refund/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": { "build": "tsc -p tsconfig.json", "typecheck": "tsc --noEmit -p tsconfig.json", "lint": "tsc --noEmit -p tsconfig.json", "start": "node dist/src/index.js" },
  "dependencies": { "@refund/domain": "*" }
}
